#include <stdio.h>
#include <stdlib.h>

typedef struct {
	void **values;
	int listc;
} list;

int
main(void) {


	list *l1=malloc(sizeof(list));
	list *l2=malloc(sizeof(list));
	l1->values=malloc(sizeof(void *)*3);
	l2->values[0]=(void *)&
return 0;
}





